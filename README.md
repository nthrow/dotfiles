# dotfiles

Configuration for fish, Neovim, tmux, git, ghostty, and ssh — deployed with GNU stow.

```sh
./bootstrap.sh            # install dependencies + stow everything into $HOME
./bootstrap.sh --no-deps  # just stow
```
